export interface buttonType {
  text: string;
  handleClick?: () => void;
  loading?: boolean;
  type: any;
}
export interface userRegisterType {
  name: string;
  email: string;
  password: string;
}
